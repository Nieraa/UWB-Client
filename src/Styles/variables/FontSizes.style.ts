import { css } from "styled-components";

const FontSizes = css`
  --lg-heading: 50px;
  --md-heading: 40px;
  --sm-heading: 24px;
  --lg-text: 21px;
  --md-text: 16px;
  --sm-text: 13px;
`;

export default FontSizes;